# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This is a new, empty Node.js project. The `package.json` is currently empty (`{}`). No source files, scripts, or dependencies have been added yet.

## Getting Started

Before developing, populate `package.json` with a name, version, scripts, and dependencies appropriate to the project type. Common next steps:

- Define the project type (e.g., CLI tool, web app, library, API server)
- Install dependencies via `npm install <package>`
- Add `scripts` to `package.json` for build, test, and lint commands
